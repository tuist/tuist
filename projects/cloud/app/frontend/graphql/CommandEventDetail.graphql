fragment CommandEventDetail on CommandEvent {
  id
  name
  subcommand
  commandArguments
  duration
  clientId
  tuistVersion
  swiftVersion
  macosVersion
  createdAt
  metadata {
    ... on CacheWarmMetadata {
      cacheableTargets
      localCacheTargetHits
      remoteCacheTargetHits
    }
  }
}
