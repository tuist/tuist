mutation InviteUser($input: InviteUserInput!) {
  inviteUser(input: $input) {
    invitee
    inviter {
      ...UserBasicInfo
    }
    organization {
      account {
        name
      }
    }
  }
}
