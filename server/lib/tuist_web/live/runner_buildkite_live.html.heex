<div id="account-runner-buildkite">
  <div data-part="page-header">
    <div data-part="heading">
      <h1 data-part="title">{dgettext("dashboard_runners", "Buildkite")}</h1>
      <span data-part="subtitle">
        {dgettext(
          "dashboard_runners",
          "Run Buildkite jobs on Tuist runners. Tuist watches the self-hosted queues in your cluster and runs what it finds there."
        )}
      </span>
    </div>
  </div>

  <.card
    title={dgettext("dashboard_runners", "Cluster")}
    icon="server"
    data-part="buildkite-card"
  >
    <.card_section>
      <.alert :if={@form_error} status="error" type="secondary" size="small" title={@form_error} />

      <.alert
        :if={@installation && @installation.last_error}
        status="error"
        type="secondary"
        size="small"
        title={@installation.last_error}
      />

      <form phx-submit="connect" data-part="buildkite-form">
        <div data-part="field">
          <span data-part="field-label">
            {dgettext("dashboard_runners", "Organization slug")}
          </span>
          <input
            id="buildkite-organization-slug"
            name="organization_slug"
            type="text"
            value={@installation && @installation.organization_slug}
            placeholder={dgettext("dashboard_runners", ~s(e.g. "acme"))}
            required
          />
        </div>

        <div data-part="field">
          <span data-part="field-label">{dgettext("dashboard_runners", "Cluster name")}</span>
          <input
            id="buildkite-cluster-name"
            name="cluster_name"
            type="text"
            value={@installation && @installation.cluster_name}
            placeholder={dgettext("dashboard_runners", ~s(e.g. "macOS"))}
          />
        </div>

        <div data-part="field">
          <span data-part="field-label">
            {dgettext("dashboard_runners", "Cluster agent token")}
          </span>
          <input
            id="buildkite-agent-token"
            name="agent_token"
            type="password"
            placeholder="bkct_..."
            required
          />
          <span data-part="field-hint">
            {dgettext(
              "dashboard_runners",
              "Create it under your cluster's Agent Tokens in Buildkite. It is stored encrypted and never shown again."
            )}
          </span>
        </div>

        <.button
          label={
            if @installation,
              do: dgettext("dashboard_runners", "Update connection"),
              else: dgettext("dashboard_runners", "Connect")
          }
          variant="primary"
          size="medium"
          type="submit"
        />
      </form>
    </.card_section>

    <.card_section :if={@installation} title={dgettext("dashboard_runners", "Status")}>
      <div data-part="field">
        <span data-part="field-label">{dgettext("dashboard_runners", "Last checked")}</span>
        <span data-part="field-value">
          {if @installation.last_polled_at,
            do: Tuist.Utilities.DateFormatter.from_now(@installation.last_polled_at),
            else: dgettext("dashboard_runners", "Not yet")}
        </span>
      </div>

      <.button
        label={dgettext("dashboard_runners", "Disconnect")}
        variant="destructive"
        size="medium"
        phx-click="disconnect"
      />
    </.card_section>
  </.card>

  <.card
    title={dgettext("dashboard_runners", "Queues")}
    icon="list_tree"
    data-part="buildkite-queues-card"
  >
    <.card_section>
      <span data-part="field-hint">
        {dgettext(
          "dashboard_runners",
          "Tuist runs a Buildkite job when its queue key matches one of your runner profiles. Create a self-hosted queue in your cluster for each profile you want to use, then target it with agents: { queue: ... } in your pipeline."
        )}
      </span>

      <ul data-part="buildkite-queue-list">
        <li :for={queue_key <- @queue_keys}>
          <code>{queue_key}</code>
        </li>
      </ul>
    </.card_section>
  </.card>
</div>
