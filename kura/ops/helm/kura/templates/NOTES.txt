1. Kura has been deployed as a StatefulSet named {{ include "kura.fullname" . }}.

2. HTTP endpoint:
   kubectl port-forward svc/{{ include "kura.fullname" . }} 4000:{{ .Values.service.httpPort }}

3. gRPC endpoint:
   kubectl port-forward svc/{{ include "kura.fullname" . }} 50051:{{ .Values.service.grpcPort }}

4. The cluster uses the headless service {{ include "kura.headlessServiceName" . }} for stable pod DNS and peer discovery.

