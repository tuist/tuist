# Provider overlay: Hetzner (via Syself Apalla cluster-stack).
# Layered after values-managed.yaml. Carries provider-wide knobs that
# every Hetzner location inherits.

persistence:
  storageClass: hcloud-volumes
  size: 200Gi
