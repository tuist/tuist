# Autoresearch: Kura bounded-resource concurrency

## Objective

Increase Kura's concurrent read and durable-write throughput by removing redundant copies, locks, synchronization, and dispatch. Do not weaken hard memory, file descriptor, temporary disk, durability, or background-work bounds.

## Metrics

- Primary: all-success ByteStream read throughput and durable ByteStream write throughput (requests per second, higher is better)
- Secondary: response latency percentiles, focused same-process microbenchmarks, hard resource bounds, crash ordering, cancellation, compile, lint, and complete unit-test status

## How to Run

`./autoresearch.sh`

## Files in Scope

- `src/reapi/service.rs`: Remote Execution ByteStream and batch response materialization
- `src/runtime.rs`: request in-flight accounting and shutdown notifications
- `src/app.rs`: shutdown-drain waiter and its notification contract
- `src/segment/reader.rs`: bounded asynchronous file reads
- `src/store.rs`: append-only segment persistence and metadata access
- `src/artifact/manifest.rs`: manifest ownership and retained allocations
- `src/accelerated_file_serving.rs`: Linux direct file serving and request classification
- `src/io/mod.rs`: bounded file descriptor operations
- `src/memory/`: admission and resource accounting affected by buffer ownership

## Off Limits

- On-disk segment, blob, or metadata formats
- Replication wire compatibility
- Unbounded queues, caches, buffers, or worker counts
- Raising resource limits to improve a benchmark
- `CHANGELOG.md` and translation files

## Constraints

- Preserve compatibility across a rolling update.
- Keep append-only files append-only and never truncate a mapped file.
- Keep exact response bytes, status codes, headers, authorization, usage, and analytics behavior.
- Keep the benchmark focused enough for repeated local runs.
- Bazel build, focused tests, formatting, and Clippy must pass before handoff.

## Research Notes

- Tokio's `ReaderStream` fills a reusable `BytesMut`, splits it into `Bytes`, and retains the remainder for reuse.
- The generated Google ByteStream `ReadResponse.data` field is a `Vec<u8>` because Prost uses that type for protocol `bytes` fields by default.
- Converting the yielded `Bytes` with `to_vec` performs a full chunk copy. Filling the final `Vec<u8>` directly can remove that pass without changing the protocol.
- Linux `sendfile` and `splice` already cover Kura's eligible plaintext artifact downloads. Remote Execution responses still require protocol framing, so the target there is one owned message buffer plus the encoder and transport buffers.
- [Tokio implements ordinary file operations on its blocking pool](https://docs.rs/tokio/latest/tokio/fs/index.html). Kura's `SegmentReader` likewise performs positional reads there, but previously returned a private vector and then copied it into the caller's asynchronous read buffer. Returning that owned vector directly removes the copy without changing runtimes.
- The separate [`tokio-uring` runtime](https://github.com/tokio-rs/tokio-uring) accepts owned buffers, but requires a ring-specific runtime and file resources. Measure the remaining blocking-pool dispatch cost after removing Kura's copy before considering that operationally larger change.
- Production Kura nodes advertise 1- or 3-gigabit-per-second public egress budgets. At the fastest rate, one 512-kibibyte chunk occupies the wire for 1.398 milliseconds; slower nodes give each handoff more time.
- Every request arrival and completion currently calls `Notify::notify_waiters`, although the only consumer waits for the in-flight count to reach zero during shutdown. An arrival can never make that condition true, and steady-state completions need no shutdown wake-up before draining begins.
- Manifest-cache hits currently clone all owned manifest strings while holding the cache's global lock. The clone is required by the public store interface, but it does not need to serialize independent allocator work across requests.
- Every chunk read from a popular segment currently takes the persistent-file handle cache's asynchronous mutex, mutates recency, and clones the same open handle. Repeated hits to the already most-recent handle do not change eviction order.
- Kura resolves [`arc-swap` 1.9.1](https://docs.rs/arc-swap/1.9.1/arc_swap/). Its primary documentation describes atomic concurrent loads and stores, says a loaded guard keeps the value alive, and records that version 1.9 corrected previously weak memory orderings. The manifest now declares 1.9.1 as the minimum version so this fast path cannot resolve to an older implementation.
- [Rust's standard library](https://doc.rust-lang.org/std/sync/struct.Arc.html) guarantees that an atomically reference-counted clone shares the same allocation and keeps the value alive until the final owner is dropped. The fast path therefore cannot expose a freed file object after a concurrent replacement or clear.
- The fast-path guard exists only long enough to compare the immutable key and clone the file owner. It is explicitly dropped before the fallback awaits the cache mutex, so there is no lock-order cycle and no guard is retained across a suspension point.
- Removal and trimming clear the fast path while holding the cache mutex that owns membership. A hot entry is published only when it remains in the bounded cache, including the zero-capacity case, so the front never owns an additional file descriptor outside the configured cache.
- A request that cloned a handle before concurrent removal may finish reading it, which matches the previous mutex path. The [Linux manual](https://man7.org/linux/man-pages/man2/unlink.2.html) specifies that unlinking the last pathname postpones file-content removal until open file descriptors are closed.
- [Tokio's mutex documentation](https://docs.rs/tokio/latest/tokio/sync/struct.Mutex.html) explains that its asynchronous mutex is more expensive than a blocking data mutex and is intended when a guard must span a suspension point. Kura still uses it for exact cache membership and recency mutations, but the atomic front is a better fit for the immutable, read-mostly hottest entry because no guard spans a suspension point.
- A production-shaped profile with four concurrent connections and 128 streams per connection found that successful reads cloned and dropped the complete metrics registry at least twice per request. Each caller only records through the registry and does not need ownership, so returning a shared reference removes the reference-count churn without changing metric lifetime or labels.
- After removing the metrics clones, the dominant high-concurrency samples are socket writes, the [Hypertext Transfer Protocol version 2](https://httpwg.org/specs/rfc9113.html) connection's shared synchronization, and positional file reads. Manifest and persistent-handle cache locks are now minor. Kura already uses the maintained [`h2` 0.4.19](https://github.com/hyperium/h2/releases/tag/v0.4.19), which includes the recent write-contention and small-frame-budget improvements. The peer controls the maximum outbound response frame size, so increasing Kura's receive-frame limit cannot remove this response-side serialization.
- The production listener accepts independent connections without sharing their protocol state machines. A bounded client pool can therefore parallelize connection-local framing and socket work while Kura's existing global response-memory and request limits continue to govern aggregate resource use.
- ByteStream resource parsing ran on every read and allocated a component vector plus an intermediate digest string. A single forward scan can retain borrowed component slices, calculate the exact normalized namespace capacity, and write the final storage key directly. This preserves the existing last-marker, separator-normalization, trailing-component, and error behavior while removing both temporary allocations.

## What's Been Tried

- Baseline repeated hits to one open persistent-file handle sustained 2,712,133.322 lookups per second across eight Tokio workers. The benchmark fixture initially proved the storage-root guard rejects paths outside configured roots; moving the synthetic handle under the configured data directory preserved that guard and measured the intended cache path.
- Added one atomic, single-entry front for the already most-recent persistent-file handle. Initial hot-key runs sustained 31,094,326.739 and 36,970,712.264 lookups per second, at least 11.464 times the mutex-only baseline. Exact recency, capacity-two eviction, trimming, removal, and zero-capacity behavior are covered by focused tests.
- An adversarial paired benchmark caught the first design republishing the front on every alternating-key miss: two cached keys fell to 0.331165 times the mutex-only path. Added a bounded 64-hit promotion threshold so new handles seed the front but cached replacements must demonstrate sustained demand. Two alternating-key runs then improved to 1.587395 and 1.542646 times the mutex-only path, while the confirmed hot-key run reached 40,070,961.666 lookups per second, 14.775 times the original baseline.
- Final same-process before-and-after benchmarks ran the mutex-only and optimized paths in alternating order. The hot-key case measured 9.927585, 10.242006, and 9.029248 times speedups, with median rates of 4,272,918.436 before and 42,345,594.465 lookups per second after. The alternating two-key case measured 1.582281, 1.566287, and 1.594304 times speedups, with median rates of 4,183,013.841 before and 6,564,421.211 lookups per second after.
- Baseline same-hot-artifact manifest-cache hits sustained 1,423,779.733 lookups per second across eight concurrent workers while cloning all manifest strings under the global lock.
- Retained cached manifests behind one shared allocation, cloned only that reference under the lock, and moved the required owned-string clone after unlock. Paired same-process runs measured 1.126040 and 1.140262 times speedups while pointer identity proved allocation reuse and retained-byte accounting includes the two reference counters.
- Baseline request accounting, with every arrival and completion notifying the shutdown waiter, sustained 279,259.648 requests per second across eight concurrent workers.
- Removed arrival notifications and limited completion notifications to the draining state. Two candidate runs sustained 349,744.257 and 360,105.299 requests per second, improving the confirmed result by 28.954 percent while focused in-flight and shutdown-drain checks passed.
- In-flight guards cloned the complete `Metrics` value even though their drop path updates only three gauges. Replaced that broad clone, which touched every shared metric family's reference count, with one shared in-flight metrics handle. Two candidate runs sustained 3,929,250.582 and 4,643,989.770 requests per second. The confirmed result is 12.896 times the notification-only result and 16.629 times the original baseline, with identical gauge updates.
- Total and public web request counts previously updated two adjacent sequentially consistent atomics on arrival and completion. Packed the two exact 32-bit counts into one 64-bit atomic, halving counter updates for public web traffic while retaining separate gauges. Two candidate runs sustained 5,222,378.062 and 7,155,864.460 requests per second; even the lower run is 12.455 percent above the previous best confirmed result.
- Added a release-mode, in-memory ByteStream chunk-materialization benchmark and a correctness test.
- Baseline: the unchanged `ReaderStream` plus `Bytes::to_vec` path reached 17,599.189 mebibytes per second. The cold optimized Bazel build took 1,142 seconds; subsequent experiments reuse its dependency cache, and build duration is not part of the throughput metric.
- Experiment: fill the generated response's final `Vec<u8>` directly through Tokio's `read_buf`, removing the intermediate `BytesMut` allocation and full-chunk copy while retaining the same chunk cap.
- The original 512 mebibyte samples completed in 0.11 to 0.23 seconds and varied from 17,185 to 29,797 mebibytes per second. Increased each sample to 8 gibibytes and the measured set to seven post-warm-up samples without increasing live memory.
- Absolute throughput still ranged from 5,926 to 16,631 mebibytes per second under unrelated host compilation. Replaced it with an interleaved original-versus-candidate speedup ratio, alternating execution order and retaining both absolute rates as secondary diagnostics.
- Identical-function calibration ranged from 0.934 to 1.067. Treat timing changes inside that band as noise and require a deterministic pointer-identity test proving that the response owns the allocation the reader filled.
- Candidate implementation fills the final response `Vec<u8>` through Tokio's uninitialized spare-capacity interface. The original copying helper remains test-only for paired measurements.
- Candidate runs: 2.747 and 4.389 times the original path. The warm confirmation measured median rates of 55,325 versus 12,238 mebibytes per second. Both exceed the identical-function calibration band by a wide margin.
- Follow-up copy audit: ByteStream writes cloned the request metadata and admission guard before consuming the stream, then cloned the resource name on every message. Unary authorization specs also owned duplicate key and hash strings, and batch reads cloned the complete digest list plus every response digest. Move these existing allocations into their consumers and keep authorization specs borrowed until an enabled policy engine builds its owned context.
- Extended request ownership through action-cache updates, missing-blob queries, and batch uploads: move decoded messages, digests, data buffers, metadata, and admission guards into their final consumers after authorization instead of cloning them to keep the request wrapper alive.
- The ownership rewrite compiled with the ByteStream candidate still at 4.467 times the original copy path. Focused ByteStream write, namespace authorization, action-cache transfer, batch transfer, and missing-empty-blob tests passed.
- Plaintext accelerator audit: one request allocated a 16 kibibyte peek buffer and a second exact-size vector merely to consume those same headers. Keep one fixed-capacity buffer per connection, reuse it across keep-alive peeks and consumption, and move denial headers instead of cloning their map.
- The connection-buffer change passed the socket-level peek/consume test, full and ranged accelerator tests, and capacity-shedding coverage. The paired ByteStream benchmark remained at 4.969 times the original copy path.
- Tonic consumes each response vector while encoding it, then yields an owned encoded frame. Hyper can retain up to its configured 512 kibibyte per-stream send buffer while the next frame is encoded. The new peak therefore has three charged owners instead of the old path's four: response vector, encoded frame, and transport buffer. At this stage, reduce only ByteStream's admission multiplier from four to three; ordinary file streams still retain their existing four-buffer model.
- Owned segment chunks now feed ByteStream, public artifact responses, and single-artifact backfill responses directly. The paired disk-backed benchmark measured a 1.419 times median speedup, and each stream's response reservation fell from four chunks to three. Spool-file responses still retain four at this point and move to owned positional reads in the later experiment below.
- Rustix now passes vector spare capacity directly to the positional read and safely extends the vector by the returned byte count. The paired low-level benchmark measured a 1.162 times median speedup over zero-initializing the same bounded allocation before the read.
- Replication upload bodies now consume the same owned chunks while preserving bandwidth reservations and forward-progress marks. The paired full body-adapter benchmark measured a 1.200 times median speedup over the copied asynchronous-reader path.
- Batched backfill spooling now writes each owned segment chunk directly into the temporary file while retaining exact-length validation. The paired reader-to-sink benchmark measured a 1.216 times median speedup over Tokio's generic copy path.
- Backfill spool responses now use the same owned positional-read stream and reserve three live chunks instead of four. The paired spool-reader benchmark measured a 1.233 times median speedup over Tokio's file stream adapter.
- Inline byte-stream consumers now yield slices of the existing reference-counted value. Pointer identity proves no byte allocation or copy; the synthetic materialization benchmark measured 372.308 times the copied path because the candidate moves no payload bytes.
- A paced benchmark staggers 32 concurrent streams across the fastest production node's 3-gigabit-per-second aggregate egress budget. Across three runs, the 95th-percentile Tokio blocking handoff was 31.083, 48.625, and 43.750 microseconds, with a median of 43.750 microseconds. That is 3.129 percent of a single chunk's 1.398-millisecond wire time; the complete cached positional read's median 95th percentile was 92.625 microseconds. Replacing Tokio would attack a small residual cost while introducing a second, Linux-specific runtime and file-resource model, so keep Tokio and optimize the remaining allocation work instead.
- Whole-artifact materialization still allocated a zero-filled result vector before the positional read overwrote every byte. On Unix, pass the vector's spare capacity to Rustix and let the operating system initialize it while preserving the exact allocation, admission bound, short-read error, and Windows fallback. Three paired 512-mebibyte runs measured 1.099118, 1.097661, and 1.103675 times speedups, a median of 1.099118 times.
- Accelerated requests previously built a complete owned authorization context even when authorization was disabled, then retained the full parsed artifact request through response admission and cloned the file handle, configured tenant, namespace, analytics key, route, and content type before transfer. Build the authorization context only when an engine exists, discard request-only fields after authorization and range resolution, move the remaining metadata and file into the transfer, borrow the configured tenant, and validate the file-owned content type inside the blocking closure. Pointer identity proves the namespace and analytics allocations move unchanged. Three paired setup runs measured 1.389168, 1.301090, and 1.386563 times speedups, a median of 1.386563 times.
- Hypertext Transfer Protocol metric recording took an owned route from the ordinary middleware but cloned it for every request's label lookup. Accelerated responses first allocated their static route and then cloned that allocation. Store metric routes as borrowed-or-owned text, move dynamic routes into the primary label lookup, and borrow accelerated route templates. Internal backfill and server-error routes still clone only when a second metric family needs the same dynamic label. Three paired steady-state runs measured 2.314626, 2.321335, and 2.286898 times speedups, a median of 2.314626 times, while keeping identical route and status label values.
- The manifest, existence, and persistent-file handle caches duplicated every key between their hash lookup and least-recently-used ordering tree. Worse, every cache hit allocated another string when it refreshed recency. Use one immutable reference-counted string allocation as both indexes' key and clone only the reference on a touch. Existence entries now retain one key allocation instead of two; manifest entries retain the identifier inside the manifest plus one shared index key instead of three copies. Three paired cache-hit runs measured 1.113830, 1.131098, and 1.119967 times speedups, a median of 1.119967 times.
- Final validation after the Tokio dispatch measurement and four kept optimizations: production compilation, formatting, and Clippy passed; the complete unit suite passed 821 tests with 0 failures and 13 intentionally ignored performance benchmarks.
- Final validation after the request-accounting synchronization pass: production compilation, formatting, and Clippy passed; the complete unit suite passed 822 tests with 0 failures and 14 intentionally ignored performance benchmarks.
- Final validation after shared manifest retention and the first hot-handle candidate: production compilation, formatting, and Clippy passed; the complete unit suite passed 825 tests with 0 failures and 16 intentionally ignored performance benchmarks.
- Final validation after bounded hot-handle promotion: production compilation, formatting, and Clippy passed; the complete unit suite passed 825 tests with 0 failures and 17 intentionally ignored performance benchmarks.
- Concurrent durable writes were limited by one segment synchronization and one RocksDB Write Ahead Log synchronization per request. Request-path writes now use monotonic durability sequences so a physical barrier covers every completed writer in its captured prefix, while each caller still waits for its own sequence. Segment batching waits one millisecond per writer already queued, capped at 40 milliseconds; metadata batching uses a one-millisecond window because segment-backed writers already arrive as a cohort. The writer counters use cancellation-safe guards and uncontended writes add no segment delay.
- The fixed 64-client workload used 5,000 unique 256-kibibyte ByteStream writes, four 64-kibibyte request messages per write, a fresh 2.5-gibibyte store, and identical 512-mebibyte soft and 768-mebibyte hard memory limits. The pre-batching branch reached 119.43 writes per second with 439-millisecond median, 1,039-millisecond 95th-percentile, and 1,334-millisecond 99th-percentile latency. The final candidate reached 226.22 writes per second with 244, 519, and 668 milliseconds respectively: 1.894 times the throughput, with the three latency percentiles reduced by 44.4, 50.0, and 49.9 percent. At concurrency one it adds no segment delay.
- A native current Buildbarn comparison on the same host reached 5,330.98 writes per second, but its documented persistent backend acknowledges the active epoch before a background synchronization whose recommended minimum interval is 300 seconds. That is not equivalent to Kura returning only after segment bytes and metadata are both durable, so the number is retained as context and not presented as a like-for-like speed claim.
- The fair read comparison preloaded the same 5,000 objects, 1.25 gibibytes of payload, and 2.5-gibibyte storage capacity into both servers, then required every request to succeed and validate its exact byte count. In sustained 100,000-read runs at concurrency 64, Kura delivered 6,914.20 reads per second versus Buildbarn's 6,105.49, a 13.2 percent lead; Kura's median, 95th-percentile, and 99th-percentile latencies were 8, 12, and 16 milliseconds versus 9, 15, and 18. At concurrency 512, Kura delivered 6,737.40 reads per second versus 6,205.27, an 8.6 percent lead, with a lower 71 versus 78 millisecond median; Buildbarn retained lower 95th and 99th percentiles at that extreme concurrency, 105 and 114 milliseconds versus 129 and 169.
- Every ByteStream response permit cloned the complete metrics registry and resolved labeled metric families again on release. Pre-resolving the two reservation gauges per protocol and retaining one narrow shared handle increased the same-process admission median from 334,579.981 to 2,342,159 admissions per second, about 7.0 times, without changing any counter, byte reservation, queue, or release ordering.
- Foreground and background response-stream semaphores already form an exact partition whose capacities sum to the configured global response byte budget. Removing the redundant third semaphore raised the isolated admission rate again to 3,377,169.700 per second, 1.230 times the paired duplicate-semaphore path and about 44 percent over the preceding retained implementation. The transient-memory semaphore still caps aggregate anonymous memory, and focused tests still fill both partition sides simultaneously and reject the next byte.
- ByteStream segment reads now use Tokio's `block_in_place` on a multithreaded runtime, which the Tokio documentation describes as handing the worker's other tasks back to the scheduler while avoiding the context switch of `spawn_blocking`. This retained blocking isolation and improved the end-to-end 512-client median by about 4 percent. Current-thread runtimes keep the direct fallback used by unit tests. The closure borrows the persistent handle rather than cloning its shared owner for an immediate positional read.
- Raising the per-connection stream ceiling on h2 0.4.16 exposed its excessive-small-DATA-frame protection: requests beyond roughly 128 failed with `ENHANCE_YOUR_CALM`. h2 0.4.19 improves the automatic frame budget based on the connection window. With that patch and a fixed 512-stream ceiling, all 100,000 requests succeeded. The stream count remains a hard bound on request and header state, while Kura's independent byte-accounted response pool bounds payload buffers.
- A fixed-memory lock-free manifest front was measured and discarded. It removed most recency-lock acquisitions but did not improve end-to-end throughput, while reserving permanent slots and widening invalidation logic. The retained shared-manifest allocation remains the smaller and safer optimization.
- Hermetiq's public Buildbarn deployment reinforces Kura's existing bounded choices: fixed storage capacity must be coupled to metadata capacity, spare blocks protect in-flight readers before reuse, reads should promote old data forward, and retention age plus metadata pressure need first-class telemetry. Kura already implements the corresponding bounded RocksDB metadata, open-handle-safe segment eviction, old-segment promotion, segment shed-age metric, and global writer serialization. Hermetiq's optional write-concurrency cap also supports retaining a bounded writer rather than creating one operating-system thread per request.
- Borrowing the input/output controller's metrics registry instead of cloning it removed the clone and drop stacks from the production-shaped profile. The focused paired benchmark improved from 2,532,786 to 405,988,328 accesses per second, a 158.695 times speedup. Median end-to-end throughput at concurrency 64 improved from 7,303.26 to 7,950.98 reads per second, 8.868 percent. At concurrency 512 the medians were within host noise. Four simultaneous 128-stream connections completed 120,000 reads at about 7,564 reads per second versus about 7,074 before, a 6.9 percent aggregate gain.
- The first temporary-allocation-free resource parser rescanned the component iterator several times and reached only 0.814 times the allocating parser's throughput, so that shape was rejected. The retained single-pass scanner reached 6,395,814.579 parses per second versus 3,314,474.877 before, a 1.925 times median speedup. Exact output and error equivalence are covered for normalized paths, repeated separators, trailing components, multiple markers, missing digests, and invalid sizes.
- Final validation after the metrics-borrow and resource-parser changes: production compilation, formatting, and Clippy passed; the complete unit suite passed 829 tests with 0 failures and 22 intentionally ignored performance benchmarks.
- Small staged writes previously allocated a zero-filled vector before reading exactly the same admitted bytes into it. A fallible spare-capacity read removes that initialization while preserving the streamed fallback if allocation fails. Paired runs improved by 1.530432 and 1.564657 times, with exact-size, truncated-source, and allocation-failure coverage. Keeping the whole upload in memory was separately rejected: 5,000 durable writes fell from 213.17 to 181.92 writes per second because removing staging stagger synchronized arrivals at the durability barrier.
- RocksDB write batches are explicitly safe to send between threads in the resolved binding. Moving the owned batch into the blocking worker instead of serializing it into a temporary byte vector and reconstructing it improved the isolated handoff by at least 1.330330 times. In a fresh 5,000-write service comparison, throughput rose from 162.84 to 176.15 writes per second, 8.17 percent, while median, 95th-percentile, and 99th-percentile latency all fell.
- The reusable 256-kibibyte streaming copy buffer now reads into spare capacity rather than zero-filling memory that the source immediately overwrites. Paired one-mebibyte copies improved by 1.183087 and 1.206748 times. The buffer remains fallible, fixed in size, and covered by the bounded replicated-write integration test.
- A 64-mebibyte Hypertext Transfer Protocol version 2 connection window was rejected. It released larger bursts into one connection and reduced median throughput from 5,184.40 to 2,720.23 reads per second while worsening all measured latency percentiles. The retained 16-mebibyte window remains above the per-stream window and schedules admitted work more smoothly. Lowering the segment durability cohort cap from 40 to 8 milliseconds was also rejected: smaller synchronization cohorts reduced median throughput from 197.05 to 188.40 durable writes per second and worsened tail latency.
- [Upstream h2 documentation](https://docs.rs/h2/latest/h2/server/struct.Builder.html) states that its maximum send buffer is per stream and stops polling body capacity when full. Capping it at 64 kibibytes reduced the charged memory for a 256-kibibyte ByteStream response from 811,008 to 606,208 bytes, 25.25 percent. Two constrained-memory order reversals improved median throughput by 19.7 and 33.3 percent; unconstrained throughput remained within host variation. The global byte semaphore, transient-memory semaphore, stream ceiling, and bounded waiter queue remain unchanged.
- Successful writes now decode the expected [Secure Hash Algorithm 256-bit](https://csrc.nist.gov/pubs/fips/180-4/upd1/final) digest into a fixed stack array and compare raw bytes, avoiding an encoded-string allocation. Paired comparisons improved by 3.845204 and 4.121954 times. Parsed ByteStream resources then retain the same digest as a byte range inside their already-owned storage key instead of allocating a second string, removing another allocation and 64 copied bytes while improving isolated construction by 1.946463 and 1.996780 times.
- Successful Remote Execution writes now update three metric handles resolved at startup. Metric recording improved by 11.720373 and 12.117167 times; a fresh 5,000-write service pair remained within noise at 222.40 and 223.99 writes per second. ByteStream write latency uses the same pattern and borrows its static route, improving isolated observation by 7.745643 and 8.052481 times while the reverse-order durable service pair differed by 1.75 percent.
- Write admission previously cloned the complete metrics aggregate twice while splitting ownership between the request extension and body wrapper, although rejection needs only three counters. Retaining one narrow shared counter group improved that clone by 80.187870 and 96.422518 times. An exact production comparison of 10,000 durable 4-kibibyte writes at concurrency 256 improved from 224.07 to 231.82 writes per second, 3.46 percent, and reduced median latency from 705 to 654 milliseconds.
- The complete metrics aggregate is now retained behind one shared immutable reference. This makes every remaining clone one reference increment instead of cloning more than one hundred metric handles. Clone throughput rose from at most 2.531 million to at least 182.115 million per second, a conservative 71.95 times improvement. Repeated 500,000-request Hypertext Transfer Protocol reads were within host noise at 77,775.51 versus 77,279.19 reads per second, with zero failures and matching median and tail latency.
- All ten fixed [Remote Execution application programming interface](https://github.com/bazelbuild/remote-apis) routes now borrow static names for request accounting. The eight metadata methods resolve their latency histogram lazily on first use, so unused methods allocate no series and active methods avoid repeated label allocation and family lookup. Paired warm observations improved by 7.924174 and 8.259050 times, while unknown routes keep the original owned-label fallback.
- Final validation after the storage, transport-accounting, and metrics-ownership passes: production compilation, formatting, and Clippy passed; the complete unit suite passed 837 tests with 0 failures and 32 intentionally ignored performance benchmarks.
- The next order-of-magnitude write opportunity is architectural rather than a larger synchronization window: reserve non-overlapping offsets in preallocated active segments, stream directly into those offsets, track incomplete reservations as bounded segment waste, and let one bounded coordinator commit completed cohorts with one segment barrier followed by one shared metadata batch and barrier. This removes the temporary-file copy and per-record metadata write while preserving byte-before-metadata crash ordering. Rotation must retain old segment handles until all reservations finish, cap simultaneously writable segments and reserved bytes, and recover the cursor from durable segment-artifact rows after restart.
- A production-listener benchmark held total concurrency at 512, used a fixed four-gibibyte runtime budget, validated every byte of 10,000 256-kibibyte reads per sample, and alternated connection-count order across three samples. In the first matrix, one, two, four, and eight connections delivered 6,051.84, 9,478.44, 10,278.98, and 10,618.39 reads per second. The confirmation delivered 4,315.81, 8,552.43, 10,495.26, and 8,489.18. Four connections were the stable choice: 1.70 and 2.43 times the corresponding one-connection throughput, with 95th-percentile latency falling from 146 to 82 milliseconds and from 207 to 81 milliseconds. Eight connections provided no repeatable advantage over four. The first attempt with the default 512-mebibyte test budget was safely refused by response admission at this concurrency, confirming that sharding does not bypass Kura's aggregate byte bound.
- Small preloaded writes now reserve non-overlapping active-segment ranges under a short mutex and issue at most 32 positioned writes concurrently. Streaming writes and memory-pressure paths retain their serial copy and page-cache-reclamation behavior. The exclusive side of a fair segment barrier prevents synchronization or rotation until every preceding positioned range is complete and its durability sequence is published, so metadata still follows durable bytes. Three strengthened isolated comparisons improved append throughput by 1.17 to 1.79 times with equivalent physical synchronization counts. Two complete artifact-store comparisons, including temporary staging, segment synchronization, metadata commit, and temporary cleanup, improved throughput from 1,632.71 to 1,721.61 and from 1,621.60 to 1,747.52 writes per second. The second comparison reduced median, 95th-percentile, and 99th-percentile latency from 33.3, 76.0, and 95.6 milliseconds to 26.2, 64.7, and 83.7 milliseconds. Concurrent writes were read back byte-for-byte, checked for non-overlapping ranges, then verified again after closing and reopening the store.
- RocksDB already groups concurrent write batches before appending them to its Write Ahead Log, so a Kura-side metadata cohort would duplicate the database's own write-group coordinator. RocksDB's established pipelined-write option was tested as the smaller alternative, but its two complete-store runs remained inside the baseline range and made the positioned path 8.0 and 12.4 percent slower than the serial control in the same runs. Keep the default ordered write queue and do not add the extra coordinator.
- The positioned-write ceiling was tested at 8, 16, 32, and 64. Eight and sixteen underfilled the storage path and reached only 0.926 and 0.966 times their paired serial controls. Sixty-four reached 1.268 times in one heavily contended run but only 0.969 times on confirmation, while doubling the maximum scheduler and disk pressure. Retain 32 as the smallest limit that produced repeated complete-store gains; validate any future configurable limit on production-shaped Linux hardware rather than tuning further on a shared development host.
- Final validation after positioned writes: production compilation, formatting, and Clippy passed; the complete unit suite passed 838 tests with 0 failures and 35 intentionally ignored performance benchmarks.
- Payloads already materialized by bounded Remote Execution batch decoding no longer take a temporary-disk round trip before their segment append. The store now shares one precheck, durability, metadata, and eviction path between file-backed and memory-backed sources, while the memory source enters the existing bounded positioned or cache-reclaiming writer directly. Two paired complete-store comparisons improved throughput from 1,505.44 to 1,975.37 and from 1,445.61 to 1,952.43 writes per second, 1.312 and 1.326 times. The second run reduced median, 95th-percentile, and 99th-percentile latency from 36.4, 79.1, and 106.9 milliseconds to 22.5, 61.1, and 72.2 milliseconds.
- Small ByteStream uploads now accumulate inside their existing source-plus-destination memory reservation and enter the direct store path; larger, bounded-policy, or failed-allocation cases keep temporary staging. The first direct candidate still entered the serial writer when uploads overlapped and produced unstable throughput plus worse tails, so it was not retained in that form. The final positioned path waits for shared segment durability and then advises away only fully covered pages in its own range before metadata publication. Two protocol-level confirmations improved throughput from 573.73 to 1,149.11 and from 754.54 to 1,224.70 durable writes per second, 2.118 and 1.589 times. Median latency fell from 95.0 to 42.1 and from 71.4 to 39.0 milliseconds; the 99th percentile fell from 201.1 to 136.6 and from 185.6 to 116.9 milliseconds.
- Authorization cache hits previously allocated three label strings and acquired three metric-family read locks per request. Resolving the finite decision, duration, and cache series at startup retains the same registered metrics and dynamic-label fallback while removing that shared lookup work. Two eight-worker paired runs improved complete hit accounting from 3.06 million to 56.10 million and from 2.44 million to 54.06 million observations per second, 17.973 and 20.949 times.
- Authorization entries and revocation markers now use Moka's synchronous concurrent cache on the hit path; only the per-question backend consultation cache remains asynchronous. Credentials are borrowed and hashed directly into a fixed 32-byte [Secure Hash Algorithm 256-bit](https://csrc.nist.gov/pubs/fips/180-4/upd1/final) key instead of being cloned, JSON-encoded, and formatted into 64 hexadecimal characters. Production contexts retain one authorization value and no unrelated header or query collections. Five paired eight-worker comparisons of the old key plus two-cache lookup against the retained path improved by 1.173 to 1.278 times; the two final confirmations improved by 17.3 and 21.4 percent. A borrowed target-key experiment was discarded after measuring 0.994 and 1.000 times the simpler owned key.
- Authorization action classification previously allocated a lowercase operation on every request and an uppercase method whenever the operation was generic. Byte-wise case-insensitive suffix and method checks preserve mixed-case behavior without allocating and remain safe for short or non-ASCII input. Three eight-worker paired runs improved classification by 9.037, 9.272, and 9.127 times; the final run used an input optimization barrier and sustained 1.88 billion decisions per second versus 205.09 million before.
- Authorization target resolution previously allocated lowercase account and namespace copies even when the request already carried their canonical form. Borrow unchanged values through the short-lived resolved request and retain owned lowercase values only when normalization changes the text; the cache key remains owned before it enters bounded storage. Canonical American Standard Code for Information Interchange text now takes one byte scan, while non-ASCII values retain the original Unicode case-expansion check. The scan alone improved by 4.253 and 5.134 times, and two final paired eight-worker account/project runs improved the complete resolver by 2.455 and 2.525 times. Payment-required grant matching now reuses that normalized account instead of allocating and lowercasing it again, improving two paired runs by 6.709 and 7.219 times. Focused tests cover canonical borrowing, mixed-case normalization, unchanged lowercase Unicode, expanding uppercase Unicode, query aliases, tenant refusal, payment classification, and both transport target rules.
- Final validation after the authorization hot-path pass: production compilation, formatting, and Clippy passed; the complete unit suite passed 842 tests with 0 failures and 43 intentionally ignored performance benchmarks.

## Next Segment

- Remove request facts the authorization policy never reads, especially the key-value body materialization currently performed once in middleware and again in the route handler.
