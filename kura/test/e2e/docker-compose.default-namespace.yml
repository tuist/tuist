services:
  kura-us:
    environment: &kura_default_namespace_env
      KURA_DEFAULT_NAMESPACE_ID: account

  kura-eu:
    environment:
      <<: *kura_default_namespace_env

  kura-ap:
    environment:
      <<: *kura_default_namespace_env
