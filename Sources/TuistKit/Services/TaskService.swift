import Foundation

struct TaskService {
    func run(
        _ task: String
    ) throws {
        // Load Tasks.swift manifest
        // Run the task using similar logic present in `ManifestLoader`
    }
}
