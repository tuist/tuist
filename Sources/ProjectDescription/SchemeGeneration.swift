import Foundation

/// Type of schemes that are generated by `SchemesGenerator`
public enum SchemeGeneration: String, Codable {
    /// Generate default schemes with custom schemes if any
    case defaultAndCustom
    /// Generate only custom schemes
    case customOnly
}
