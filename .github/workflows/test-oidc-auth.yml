name: Test OIDC Authentication

on:
  push:
    branches:
      - feat/oidc-tokens
  workflow_dispatch:
    inputs:
      tuist_url:
        description: "Tuist server URL"
        required: false
        default: "https://staging.tuist.dev"

permissions:
  id-token: write
  contents: read

jobs:
  test-oidc:
    name: Test OIDC Token Exchange
    runs-on: ubuntu-latest
    steps:
      - name: Get GitHub OIDC Token
        id: get-oidc-token
        run: |
          echo "Requesting OIDC token from GitHub..."
          OIDC_TOKEN=$(curl -sS -H "Authorization: bearer $ACTIONS_ID_TOKEN_REQUEST_TOKEN" \
            "$ACTIONS_ID_TOKEN_REQUEST_URL&audience=tuist" | jq -r '.value')

          if [ -z "$OIDC_TOKEN" ] || [ "$OIDC_TOKEN" = "null" ]; then
            echo "Failed to get OIDC token from GitHub"
            exit 1
          fi

          echo "Successfully obtained OIDC token"
          echo "oidc_token=$OIDC_TOKEN" >> $GITHUB_OUTPUT

      - name: Exchange OIDC Token for Tuist Token
        id: exchange-token
        env:
          TUIST_URL: ${{ inputs.tuist_url }}
          OIDC_TOKEN: ${{ steps.get-oidc-token.outputs.oidc_token }}
        run: |
          echo "Exchanging OIDC token at $TUIST_URL/api/oidc/token..."

          RESPONSE=$(curl -sS -w "\n%{http_code}" -X POST "$TUIST_URL/api/oidc/token" \
            -H "Content-Type: application/json" \
            -d "{\"token\": \"$OIDC_TOKEN\"}")

          HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
          BODY=$(echo "$RESPONSE" | sed '$d')

          echo "HTTP Status: $HTTP_CODE"
          echo "Response: $BODY"

          if [ "$HTTP_CODE" != "200" ]; then
            echo "Token exchange failed with status $HTTP_CODE"
            echo "$BODY" | jq -r '.message // "Unknown error"'
            exit 1
          fi

          TUIST_TOKEN=$(echo "$BODY" | jq -r '.access_token')
          TOKEN_TYPE=$(echo "$BODY" | jq -r '.token_type')
          EXPIRES_IN=$(echo "$BODY" | jq -r '.expires_in')

          if [ -z "$TUIST_TOKEN" ] || [ "$TUIST_TOKEN" = "null" ]; then
            echo "Failed to extract access_token from response"
            exit 1
          fi

          echo "Successfully obtained Tuist token"
          echo "Token type: $TOKEN_TYPE"
          echo "Expires in: $EXPIRES_IN seconds"

          # Mask the token in logs
          echo "::add-mask::$TUIST_TOKEN"
          echo "tuist_token=$TUIST_TOKEN" >> $GITHUB_OUTPUT

      - name: Verify Tuist Token Works
        env:
          TUIST_URL: ${{ inputs.tuist_url }}
          TUIST_TOKEN: ${{ steps.exchange-token.outputs.tuist_token }}
        run: |
          echo "Testing Tuist token by making an authenticated request..."

          # Try to access a protected endpoint (this will depend on what endpoints exist)
          # For now, we just verify the token was obtained successfully
          echo "OIDC authentication test completed successfully!"
          echo ""
          echo "The token can be used with:"
          echo "  export TUIST_CONFIG_TOKEN=\$TUIST_TOKEN"
          echo "  tuist cache warm"
