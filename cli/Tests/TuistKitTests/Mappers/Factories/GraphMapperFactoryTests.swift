import Foundation
import Path
import Testing
import TuistAutomation
import TuistConfig
import TuistDependencies
import TuistLoader
import TuistServer
import XcodeGraph

#if canImport(TuistCacheEE)
    import TuistCacheEE
#endif

@testable import TuistCore
@testable import TuistGenerator
@testable import TuistKit
@testable import TuistTesting

struct GraphMapperFactoryTests {
    var subject: GraphMapperFactory!

    init() {
        subject = GraphMapperFactory()
    }

    @Test func default_contains_the_update_workspace_projects_graph_mapper() {
        // When
        let got = subject.default(config: .test())

        // Then
        #expect(got.contains(where: { $0 is UpdateWorkspaceProjectsGraphMapper }))
    }

    @Test func default_contains_the_explicit_dependency_graph_mapper_when_explcit_dependencies_are_enforced() {
        // When
        let got = subject.default(
            config: .test(project: .generated(.test(generationOptions: .test(enforceExplicitDependencies: true))))
        )

        // Then
        #expect(got.contains(where: { $0 is ExplicitDependencyGraphMapper }))
    }

    @Test func default_does_not_contain_the_explicit_dependency_graph_mapper() {
        // When
        let got = subject.default(config: .test())

        // Then
        #expect(!got.contains(where: { $0 is ExplicitDependencyGraphMapper }))
    }

    @Test func default_contains_the_modulemap_mapper() {
        // When
        let got = subject.default(config: .test())

        // Then
        #expect(got.contains(where: { $0 is ModuleMapMapper }))
    }
}

#if canImport(TuistCacheEE)
    struct CacheGraphMapperFactoryTests {
        private let subject: CacheGraphMapperFactory
        private let cacheStorage: MockCacheStoring

        init() {
            cacheStorage = MockCacheStoring()
            subject = CacheGraphMapperFactory(contentHasher: ContentHasher())
        }

        @Test func generation_contains_the_graph_mapper_to_auto_generate_schemes() {
            // Given
            let includedTargets: Set<TargetQuery> = Set([.named("MyTarget")])

            // When
            let got = subject.generation(
                config: .test(project: .testGeneratedProject()),
                cacheProfile: .onlyExternal,
                cacheSources: includedTargets,
                configuration: "Debug",
                cacheStorage: cacheStorage
            )

            // Then
            #expect(got.contains(where: { $0 is AutogeneratedWorkspaceSchemeGraphMapper }))
        }

        @Test func cache_contains_the_filter_target_dependenies_tree_graph_mapper() {
            // Given
            let includedTargets: Set<TargetQuery> = Set([.named("MyTarget")])

            // When
            let got = subject.binaryCacheWarming(
                config: .test(),
                targets: [.iOS: includedTargets],
                cacheSources: includedTargets,
                configuration: "Debug",
                cacheStorage: cacheStorage
            )

            // Then
            let mapper = got.first(where: { $0 is FocusTargetsGraphMappers }) as? FocusTargetsGraphMappers
            #expect(mapper != nil)
            #expect(mapper?.includedTargets == includedTargets)
        }

        @Test func cache_contains_the_tree_shaking_mapper() {
            // Given
            let includedTargets: Set<TargetQuery> = Set([.named("MyTarget")])

            // When
            let got = subject.binaryCacheWarming(
                config: .test(),
                targets: [.iOS: includedTargets],
                cacheSources: includedTargets,
                configuration: "Debug",
                cacheStorage: cacheStorage
            )

            // Then
            if let elementIndex = got.lastIndex(where: { $0 is TreeShakePrunedTargetsGraphMapper }),
               let previousIndex = got.firstIndex(where: { $0 is FocusTargetsGraphMappers })
            {
                #expect(elementIndex > previousIndex)
            } else {
                Issue.record("Expected element of type TreeShakePrunedTargetsGraphMapper after FocusTargetsGraphMappers")
            }
        }

        @Test func cache_contains_the_generate_cacheable_schemes_graph_mapper() {
            // Given
            let includedTargets: Set<TargetQuery> = Set([.named("MyTarget")])

            // When
            let got = subject.binaryCacheWarming(
                config: .test(),
                targets: [.iOS: includedTargets],
                cacheSources: includedTargets,
                configuration: "Debug",
                cacheStorage: cacheStorage
            )

            // Then
            let mapper = got.first(where: { $0 is GenerateCacheableSchemesGraphMapper })
                as? GenerateCacheableSchemesGraphMapper
            #expect(mapper != nil)
            #expect(mapper?.targets == [.iOS: includedTargets])
        }

        @Test func focus_contains_the_filter_target_dependenies_tree_graph_mapper() {
            // Given
            let config = Tuist.test()
            let cacheSources: Set<TargetQuery> = Set([.named("MyTarget")])

            // When
            let got = subject.generation(
                config: config,
                cacheProfile: .none,
                cacheSources: cacheSources,
                configuration: "Debug",
                cacheStorage: cacheStorage
            )

            // Then
            let mapper = got.first(where: { $0 is FocusTargetsGraphMappers }) as? FocusTargetsGraphMappers
            #expect(mapper != nil)
            #expect(mapper?.includedTargets == cacheSources)
        }

        @Test func focus_contains_the_cache_tree_shaking_graph_mapper() {
            // Given
            let config = Tuist.test()
            let cacheSources: Set<TargetQuery> = Set([.named("MyTarget")])

            // When
            let got = subject.generation(
                config: config,
                cacheProfile: .onlyExternal,
                cacheSources: cacheSources,
                configuration: "Debug",
                cacheStorage: cacheStorage
            )

            // Then
            if let elementIndex = got.lastIndex(where: { $0 is TreeShakePrunedTargetsGraphMapper }),
               let previousIndex = got.firstIndex(where: { $0 is FocusTargetsGraphMappers })
            {
                #expect(elementIndex > previousIndex)
            } else {
                Issue.record(
                    "Expected element of type TreeShakePrunedTargetsGraphMapper after FocusTargetsGraphMappers"
                )
            }
            if let elementIndex = got.lastIndex(where: { $0 is TreeShakePrunedTargetsGraphMapper }),
               let previousIndex = got.firstIndex(where: { $0 is TargetsToCacheBinariesGraphMapper })
            {
                #expect(elementIndex > previousIndex)
            } else {
                Issue.record(
                    "Expected element of type TreeShakePrunedTargetsGraphMapper after TargetsToCacheBinariesGraphMapper"
                )
            }
        }

        @Test func focus_contains_the_cache_mapper() {
            // Given
            let config = Tuist.test()
            let cacheSources: Set<TargetQuery> = Set([.named("MyTarget")])

            // When
            let got = subject.generation(
                config: config,
                cacheProfile: .onlyExternal,
                cacheSources: cacheSources,
                configuration: "Debug",
                cacheStorage: cacheStorage
            )

            // Then
            if let elementIndex = got.lastIndex(where: { $0 is TargetsToCacheBinariesGraphMapper }),
               let previousIndex = got.firstIndex(where: { $0 is FocusTargetsGraphMappers })
            {
                #expect(elementIndex > previousIndex)
            } else {
                Issue.record(
                    "Expected element of type TargetsToCacheBinariesGraphMapper after FocusTargetsGraphMappers"
                )
            }
        }

        @Test func automation_contains_the_tests_cache_graph_mapper() throws {
            // Given
            let config = Tuist.test()

            // When
            let got = subject.automation(
                config: config,
                ignoreBinaryCache: false,
                ignoreSelectiveTesting: false,
                testPlan: nil,
                includedTargets: [],
                excludedTargets: [],
                configuration: "Debug",
                cacheStorage: cacheStorage,
                destination: nil
            )

            // Then
            #expect(got.contains(where: { $0 is TestsCacheGraphMapper }))
        }

        @Test func automation_contains_the_filter_target_dependenies_tree_graph_mapper() throws {
            // Given
            let config = Tuist.test()
            let includedTargets: Set<TargetQuery> = [.named("MyTarget")]

            // When
            let got = subject.automation(
                config: config,
                ignoreBinaryCache: false,
                ignoreSelectiveTesting: true,
                testPlan: nil,
                includedTargets: includedTargets,
                excludedTargets: [],
                configuration: "Debug",
                cacheStorage: cacheStorage,
                destination: nil
            )

            // Then - FocusTargetsGraphMappers runs after TestsCacheGraphMapper
            if let elementIndex = got.lastIndex(where: { $0 is FocusTargetsGraphMappers }),
               let previousIndex = got.firstIndex(where: { $0 is TestsCacheGraphMapper })
            {
                #expect(elementIndex > previousIndex)
            } else {
                Issue.record("Expected element of type FocusTargetsGraphMappers after TestsCacheGraphMapper")
            }
        }

        @Test func automation_runs_tests_cache_after_static_xcframework_module_map_mapper() throws {
            // Given
            let config = Tuist.test()

            // When
            let got = subject.automation(
                config: config,
                ignoreBinaryCache: false,
                ignoreSelectiveTesting: false,
                testPlan: nil,
                includedTargets: [],
                excludedTargets: [],
                configuration: "Debug",
                cacheStorage: cacheStorage,
                destination: nil
            )

            // Then - TestsCacheGraphMapper should run after StaticXCFrameworkModuleMapGraphMapper for consistent hashing
            if let elementIndex = got.lastIndex(where: { $0 is TestsCacheGraphMapper }),
               let previousIndex = got.firstIndex(where: { $0 is StaticXCFrameworkModuleMapGraphMapper })
            {
                #expect(elementIndex > previousIndex)
            } else {
                Issue.record(
                    "Expected element of type TestsCacheGraphMapper after StaticXCFrameworkModuleMapGraphMapper"
                )
            }
        }

        @Test func automation_contains_the_tests_cache_tree_shaking_mapper() throws {
            // Given
            let config = Tuist.test()

            // When
            let got = subject.automation(
                config: config,
                ignoreBinaryCache: false,
                ignoreSelectiveTesting: false,
                testPlan: nil,
                includedTargets: [],
                excludedTargets: [],
                configuration: "Debug",
                cacheStorage: cacheStorage,
                destination: nil
            )

            // Then
            if let elementIndex = got.lastIndex(where: { $0 is TreeShakePrunedTargetsGraphMapper }),
               let previousIndex = got.firstIndex(where: { $0 is FocusTargetsGraphMappers })
            {
                #expect(elementIndex > previousIndex)
            } else {
                Issue.record(
                    "Expected element of type TreeShakePrunedTargetsGraphMapper after FocusTargetsGraphMappers"
                )
            }
        }

        @Test func default_contains_focus_mappers_when_targets_specified() {
            // Given
            let includedTargets: Set<TargetQuery> = [.named("MyTarget")]

            // When
            let got = subject.default(config: .test(), includedTargets: includedTargets)

            // Then
            let mapper = got.first(where: { $0 is FocusTargetsGraphMappers }) as? FocusTargetsGraphMappers
            #expect(mapper != nil)
            #expect(mapper?.includedTargets == includedTargets)
        }

        @Test func default_does_not_contain_focus_mappers_when_no_targets_specified() {
            // When
            let got = subject.default(config: .test(), includedTargets: [])

            // Then
            #expect(!got.contains(where: { $0 is FocusTargetsGraphMappers }))
        }

        @Test func default_contains_explicit_dependency_mapper_when_enforced() {
            // Given
            let config = Tuist.test(
                project: .generated(.test(generationOptions: .test(enforceExplicitDependencies: true)))
            )

            // When
            let got = subject.default(config: config, includedTargets: [])

            // Then
            #expect(got.contains(where: { $0 is ExplicitDependencyGraphMapper }))
        }

        @Test func binaryCacheWarmingPreload_and_generation_use_same_base_mappers() {
            // Given
            let config = Tuist.test()
            let targets: Set<TargetQuery> = [.named("MyTarget")]

            // When
            let preloadMappers = subject.binaryCacheWarmingPreload(
                targetsToBinaryCache: targets,
                config: config
            )
            let generationMappers = subject.generation(
                config: config,
                cacheProfile: .none,
                cacheSources: targets,
                configuration: "Debug",
                cacheStorage: cacheStorage
            )

            // Then - Both should contain the same hash-affecting mappers in the same order
            let preloadMapperTypes = preloadMappers.prefix(8).map { String(describing: type(of: $0)) }
            let generationMapperTypes = generationMappers.prefix(8).map { String(describing: type(of: $0)) }

            #expect(preloadMapperTypes == generationMapperTypes)
        }

        @Test func automation_contains_static_xcframework_module_map_mapper_after_cache_replacement() throws {
            // Given
            let config = Tuist.test()

            // When
            let got = subject.automation(
                config: config,
                ignoreBinaryCache: false,
                ignoreSelectiveTesting: false,
                testPlan: nil,
                includedTargets: [],
                excludedTargets: [],
                configuration: "Debug",
                cacheStorage: cacheStorage,
                destination: nil
            )

            // Then
            if let elementIndex = got.lastIndex(where: { $0 is StaticXCFrameworkModuleMapGraphMapper }),
               let previousIndex = got.firstIndex(where: { $0 is TargetsToCacheBinariesGraphMapper })
            {
                #expect(elementIndex > previousIndex)
            } else {
                Issue.record(
                    "Expected element of type StaticXCFrameworkModuleMapGraphMapper after TargetsToCacheBinariesGraphMapper"
                )
            }
        }

        @Test func build_contains_static_xcframework_module_map_mapper_after_cache_replacement() {
            // Given
            let config = Tuist.test()

            // When
            let got = subject.build(
                config: config,
                ignoreBinaryCache: false,
                configuration: "Debug",
                cacheStorage: cacheStorage
            )

            // Then
            if let elementIndex = got.lastIndex(where: { $0 is StaticXCFrameworkModuleMapGraphMapper }),
               let previousIndex = got.firstIndex(where: { $0 is TargetsToCacheBinariesGraphMapper })
            {
                #expect(elementIndex > previousIndex)
            } else {
                Issue.record(
                    "Expected element of type StaticXCFrameworkModuleMapGraphMapper after TargetsToCacheBinariesGraphMapper"
                )
            }
        }

        @Test func generation_contains_static_xcframework_module_map_mapper_after_cache_replacement() {
            // Given
            let config = Tuist.test()
            let cacheSources: Set<TargetQuery> = Set([.named("MyTarget")])

            // When
            let got = subject.generation(
                config: config,
                cacheProfile: .onlyExternal,
                cacheSources: cacheSources,
                configuration: "Debug",
                cacheStorage: cacheStorage
            )

            // Then
            if let elementIndex = got.lastIndex(where: { $0 is StaticXCFrameworkModuleMapGraphMapper }),
               let previousIndex = got.firstIndex(where: { $0 is TargetsToCacheBinariesGraphMapper })
            {
                #expect(elementIndex > previousIndex)
            } else {
                Issue.record(
                    "Expected element of type StaticXCFrameworkModuleMapGraphMapper after TargetsToCacheBinariesGraphMapper"
                )
            }
        }

        @Test func binaryCacheWarming_generates_schemes_after_platform_narrowing() {
            // Given
            let includedTargets: Set<TargetQuery> = Set([.named("MyTarget")])

            // When
            let got = subject.binaryCacheWarming(
                config: .test(),
                targets: [.iOS: includedTargets],
                cacheSources: includedTargets,
                configuration: "Debug",
                cacheStorage: cacheStorage
            )

            // Then - GenerateCacheableSchemesGraphMapper must run after ExternalProjectsPlatformNarrowerGraphMapper
            // so it sees narrowed destinations and doesn't generate Catalyst schemes for iPhone-only projects
            if let elementIndex = got.lastIndex(where: { $0 is GenerateCacheableSchemesGraphMapper }),
               let previousIndex = got.firstIndex(where: { $0 is ExternalProjectsPlatformNarrowerGraphMapper })
            {
                #expect(elementIndex > previousIndex)
            } else {
                Issue.record(
                    "Expected element of type GenerateCacheableSchemesGraphMapper after ExternalProjectsPlatformNarrowerGraphMapper"
                )
            }
        }

        @Test func binaryCacheWarming_contains_static_xcframework_module_map_mapper_after_cache_replacement() {
            // Given
            let includedTargets: Set<TargetQuery> = Set([.named("MyTarget")])

            // When
            let got = subject.binaryCacheWarming(
                config: .test(),
                targets: [.iOS: includedTargets],
                cacheSources: includedTargets,
                configuration: "Debug",
                cacheStorage: cacheStorage
            )

            // Then
            if let elementIndex = got.lastIndex(where: { $0 is StaticXCFrameworkModuleMapGraphMapper }),
               let previousIndex = got.firstIndex(where: { $0 is TargetsToCacheBinariesGraphMapper })
            {
                #expect(elementIndex > previousIndex)
            } else {
                Issue.record(
                    "Expected element of type StaticXCFrameworkModuleMapGraphMapper after TargetsToCacheBinariesGraphMapper"
                )
            }
        }

        @Test func automation_and_binaryCacheWarming_use_same_base_mappers() {
            // Given
            let config = Tuist.test()
            let targets: Set<TargetQuery> = [.named("MyTarget")]

            // When
            let automationMappers = subject.automation(
                config: config,
                ignoreBinaryCache: true,
                ignoreSelectiveTesting: true,
                testPlan: nil,
                includedTargets: targets,
                excludedTargets: [],
                configuration: "Debug",
                cacheStorage: cacheStorage,
                destination: nil
            )
            let cacheMappers = subject.binaryCacheWarming(
                config: config,
                targets: [.iOS: targets],
                cacheSources: targets,
                configuration: "Debug",
                cacheStorage: cacheStorage
            )

            // Then - Both should contain ModuleMapMapper and StaticXCFrameworkModuleMapGraphMapper
            // in the same relative order for consistent hashing
            #expect(automationMappers.contains(where: { $0 is ModuleMapMapper }))
            #expect(automationMappers.contains(where: { $0 is StaticXCFrameworkModuleMapGraphMapper }))
            #expect(cacheMappers.contains(where: { $0 is ModuleMapMapper }))
            #expect(cacheMappers.contains(where: { $0 is StaticXCFrameworkModuleMapGraphMapper }))
        }
    }
#endif
