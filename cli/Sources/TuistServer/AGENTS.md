# TuistServer (Server Integration)

This module handles CLI integration with the Tuist Server APIs.

## Responsibilities
- Authenticate and communicate with the server.
- Map CLI actions to server-side operations (projects, previews, analytics, registry).

## Related Context
- Server codebase: `server/AGENTS.md`
