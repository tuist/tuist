apiVersion: 1

datasources:
  - name: Prometheus (local, scraping staging)
    uid: tuist-dev-prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
    jsonData:
      timeInterval: 15s
