defmodule TuistRegistry do
  @moduledoc """
  Top-level OTP application for the standalone registry service.

  Hosts the Swift Package Registry surface under `/swift` and is
  designed to grow into a multi-ecosystem package registry over time.
  """
end
