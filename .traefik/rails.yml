http:
  routers:
    rails:
      rule: "PathPrefix(`/`)"
      service: rails
      # middlewares:
      #   - strip-double-slash

  services:
    rails:
      loadBalancer:
        servers:
        - url: "http://127.0.0.1:3000/"
        passHostHeader: true

  # middlewares:
  #   strip-double-slash:
  #     replacePathRegex:
  #       regex: "^//([^/].*)"
  #       replacement: "/$$1"
