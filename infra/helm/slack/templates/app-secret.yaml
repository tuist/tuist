{{- $secretName := include "slack.appSecretName" . -}}
{{- $existingSecret := lookup "v1" "Secret" .Release.Namespace $secretName -}}
{{- $existingData := dict -}}
{{- if and $existingSecret $existingSecret.data }}
  {{- $existingData = $existingSecret.data }}
{{- end }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ $secretName }}
  labels:
    {{- include "slack.labels" . | nindent 4 }}
    app.kubernetes.io/component: app
type: Opaque
stringData:
  secret-key-base: {{ .Values.secretKeyBase | default (get $existingData "secret-key-base" | default "" | b64dec) | default (randAlphaNum 64) | quote }}
  release-cookie: {{ .Values.releaseCookie | default (get $existingData "release-cookie" | default "" | b64dec) | default (randAlphaNum 64) | quote }}
