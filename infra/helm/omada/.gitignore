# Subchart tarballs are pulled by `helm dependency update` before install.
# Chart.lock pins the exact version, so the tarball is a reproducible
# build artifact — no need to vendor it in git.
charts/*.tgz
