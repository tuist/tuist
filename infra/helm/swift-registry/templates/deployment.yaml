{{- $host := .Values.host | default .Values.ingress.host -}}
{{- $publicHost := required "host or ingress.host is required" $host -}}
{{- $imageTag := required "image.tag is required" .Values.image.tag -}}
{{- $podLabels := merge (dict) (.Values.global.podLabels | default (dict)) (.Values.podLabels | default (dict)) -}}
{{- $nodeSelector := merge (dict) (.Values.global.nodeSelector | default (dict)) (.Values.nodeSelector | default (dict)) -}}
{{- $tolerations := concat (.Values.global.tolerations | default (list)) (.Values.tolerations | default (list)) -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "swift-registry.fullname" . }}
  labels:
    {{- include "swift-registry.labels" . | nindent 4 }}
    app.kubernetes.io/component: app
spec:
  replicas: {{ .Values.replicaCount }}
  strategy:
    type: {{ .Values.deploymentStrategy.type }}
  selector:
    matchLabels:
      {{- include "swift-registry.componentLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "swift-registry.componentLabels" . | nindent 8 }}
        {{- with $podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      terminationGracePeriodSeconds: {{ .Values.terminationGracePeriodSeconds }}
      serviceAccountName: {{ include "swift-registry.serviceAccountName" . }}
      automountServiceAccountToken: {{ .Values.automountServiceAccountToken }}
      {{- include "swift-registry.podScheduling" . | nindent 6 }}
      {{- with .Values.podSecurityContext }}
      securityContext:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with $tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      containers:
        - name: swift-registry
          image: "{{ .Values.image.repository }}:{{ $imageTag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          securityContext:
            {{- toYaml .Values.containerSecurityContext | nindent 12 }}
          ports:
            - name: http
              containerPort: {{ .Values.service.targetPort }}
          env:
            - name: MIX_ENV
              value: "prod"
            - name: PUBLIC_HOST
              value: {{ $publicHost | quote }}
            - name: PORT
              value: {{ printf "%v" .Values.service.targetPort | quote }}
            - name: SERVER_URL
              value: {{ .Values.serverUrl | quote }}
            - name: SENTRY_ENV
              value: {{ .Values.sentryEnv | quote }}
            - name: DEPLOY_ENV
              value: {{ .Values.deployEnv | quote }}
            - name: POOL_SIZE
              value: {{ .Values.poolSize | quote }}
            - name: STORAGE_DIR
              value: {{ .Values.storageDir | quote }}
            {{- with .Values.s3.endpoint }}
            - name: S3_ENDPOINT
              value: {{ . | quote }}
            {{- end }}
            {{- with .Values.s3.protocol }}
            - name: S3_PROTOCOL
              value: {{ . | quote }}
            {{- end }}
            - name: S3_VIRTUAL_HOST
              value: {{ ternary "true" "false" .Values.s3.virtualHost | quote }}
            {{- with .Values.s3.caCertPem }}
            - name: S3_CA_CERT_PEM
              value: {{ . | quote }}
            {{- end }}
            - name: REGISTRY_SYNC_ALLOWLIST
              value: {{ .Values.registrySyncAllowlist | default "" | quote }}
            - name: REGISTRY_SYNC_ENABLED
              value: {{ ternary "true" "false" .Values.registrySyncEnabled | quote }}
            {{- with .Values.observability.otelEndpoint }}
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: {{ . | quote }}
            {{- end }}
            {{- with .Values.observability.lokiUrl }}
            - name: LOKI_URL
              value: {{ . | quote }}
            {{- end }}
            - name: SECRET_KEY_BASE
              valueFrom:
                secretKeyRef:
                  name: {{ include "swift-registry.appSecretName" . }}
                  key: secret-key-base
            - name: RELEASE_COOKIE
              valueFrom:
                secretKeyRef:
                  name: {{ include "swift-registry.appSecretName" . }}
                  key: release-cookie
            {{- with .Values.extraEnv }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          envFrom:
            - secretRef:
                name: {{ include "swift-registry.runtimeSecretName" . }}
            {{- with .Values.extraEnvFrom }}
            {{- toYaml . | nindent 12 }}
            {{- end }}
          startupProbe:
            httpGet:
              path: {{ .Values.probes.startup.path }}
              port: http
            initialDelaySeconds: {{ .Values.probes.startup.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.startup.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.startup.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.startup.failureThreshold }}
          readinessProbe:
            httpGet:
              path: {{ .Values.probes.readiness.path }}
              port: http
            initialDelaySeconds: {{ .Values.probes.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.readiness.failureThreshold }}
          livenessProbe:
            httpGet:
              path: {{ .Values.probes.liveness.path }}
              port: http
            initialDelaySeconds: {{ .Values.probes.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.probes.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.probes.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.probes.liveness.failureThreshold }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          volumeMounts:
            - name: data
              mountPath: /data
            - name: storage
              mountPath: {{ .Values.storageDir }}
            - name: tmp
              mountPath: /tmp
      volumes:
        - name: data
          persistentVolumeClaim:
            claimName: {{ include "swift-registry.dataClaimName" . }}
        - name: storage
          persistentVolumeClaim:
            claimName: {{ include "swift-registry.storageClaimName" . }}
        - name: tmp
          emptyDir: {}
