tuistEnv: staging
publicHostname: kube-staging.tuist.dev
extAuthzUrl: "http://ops.tuist.ts.net/api/v1/policy"

pomerium:
  config:
    routes:
      - from: "https://kube-staging.tuist.dev"
        to: "https://kubernetes.default.svc:443"
        tls_skip_verify: true
        pass_identity_headers: true
        preserve_host_header: false
        timeout: 90s
        policy:
          - allow:
              and:
                - domain:
                    is: tuist.dev
    authenticate:
      url: "https://authenticate.kube-staging.tuist.dev"
  ingress:
    hosts:
      - host: kube-staging.tuist.dev
        paths:
          - path: /
            pathType: ImplementationSpecific
      - host: authenticate.kube-staging.tuist.dev
        paths:
          - path: /
            pathType: ImplementationSpecific
    tls:
      - hosts:
          - kube-staging.tuist.dev
          - authenticate.kube-staging.tuist.dev
        secretName: pomerium-staging-tls
