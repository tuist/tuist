tuistEnv: canary
publicHostname: kube-canary.tuist.dev
extAuthzUrl: "http://ops.tuist.ts.net/api/v1/policy"

pomerium:
  config:
    routes:
      - from: "https://kube-canary.tuist.dev"
        to: "https://kubernetes.default.svc:443"
        tls_skip_verify: true
        pass_identity_headers: true
        preserve_host_header: false
        timeout: 90s
        policy:
          - allow:
              and:
                - domain:
                    is: tuist.dev
    authenticate:
      url: "https://authenticate.kube-canary.tuist.dev"
  ingress:
    hosts:
      - host: kube-canary.tuist.dev
        paths:
          - path: /
            pathType: ImplementationSpecific
      - host: authenticate.kube-canary.tuist.dev
        paths:
          - path: /
            pathType: ImplementationSpecific
    tls:
      - hosts:
          - kube-canary.tuist.dev
          - authenticate.kube-canary.tuist.dev
        secretName: pomerium-canary-tls
