{{- if and .Values.nooraStorybook.enabled .Values.nooraStorybook.ingress.enabled }}
{{- $host := required "nooraStorybook.ingress.host is required when nooraStorybook.ingress.enabled is true" .Values.nooraStorybook.ingress.host -}}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "tuist.componentName" (dict "root" . "component" "noora-storybook") }}
  labels:
    {{- include "tuist.labels" . | nindent 4 }}
    app.kubernetes.io/component: noora-storybook
  {{- with .Values.nooraStorybook.ingress.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- with .Values.nooraStorybook.ingress.className }}
  ingressClassName: {{ . | quote }}
  {{- end }}
  {{- if .Values.nooraStorybook.ingress.tlsSecretName }}
  tls:
    - hosts:
        - {{ $host | quote }}
      secretName: {{ .Values.nooraStorybook.ingress.tlsSecretName | quote }}
  {{- end }}
  rules:
    - host: {{ $host | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "tuist.componentName" (dict "root" . "component" "noora-storybook") }}
                port:
                  number: {{ .Values.nooraStorybook.service.port }}
{{- end }}
