apiVersion: v1
kind: ConfigMap
metadata:
  name: coredns
  namespace: kube-system
data:
  Corefile: |
    # Forked from the Kubernetes 1.34.6 kubeadm default for CoreDNS 1.12.1.
    # The explicit, provider-neutral upstreams are the intentional delta.
    # Tailscale egress uses operator-created cluster.local proxy Services,
    # so tailnet names do not depend on these public upstream resolvers.
    .:53 {
        errors
        health {
           lameduck 5s
        }
        ready
        kubernetes cluster.local in-addr.arpa ip6.arpa {
           pods insecure
           fallthrough in-addr.arpa ip6.arpa
           ttl 30
        }
        prometheus :9153
        forward . 1.1.1.1 8.8.8.8 {
           max_concurrent 1000
        }
        cache 30 {
           disable success cluster.local
           disable denial cluster.local
        }
        loop
        reload
        loadbalance
    }
