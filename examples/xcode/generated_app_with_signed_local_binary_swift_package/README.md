# App With Signed Local Binary Swift Package

This example validates that Tuist can propagate an expected signature configured in `PackageSettings`
to a binary XCFramework target coming from a local Swift package dependency.
