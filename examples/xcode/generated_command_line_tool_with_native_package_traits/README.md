# Native package traits

This fixture verifies that traits declared through `Project.packages` are written to the generated Xcode project. The command-line tool only compiles when the `NativeIntegration` package trait is enabled.
