defmodule TuistJit do
  @moduledoc """
  TuistJit is the just-in-time elevation service for `kubectl` access to
  Tuist's Kubernetes clusters. Decoupled from the customer-facing Tuist
  server so internal cluster access is independent of customer-facing
  service health.

  See `AGENTS.md` for the architecture and intent.
  """
end
