service: proxy
image: tuist/proxy

servers:
  web:
    - 5.161.218.106

proxy:
  ssl: true
  host: staging.tuist.dev
  app_port: 80

registry:
  server: ghcr.io
  username: cschmatzler
  password:
    - KAMAL_REGISTRY_PASSWORD

builder:
  arch: amd64
  context: .
  dockerfile: Dockerfile
