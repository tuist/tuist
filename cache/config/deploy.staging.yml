servers:
  web:
    hosts:
      - cache-eu-central-staging.tuist.dev
      - cache-us-east-staging.tuist.dev

env:
  clear:
    SERVER_URL: https://staging.tuist.dev
    SENTRY_ENV: stag
    DEPLOY_ENV: stag
    DISTRIBUTED_KV_POOL_SIZE: "10"
    KEY_VALUE_MODE: distributed
