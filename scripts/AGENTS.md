# Scripts

This directory contains operational helper scripts for repository maintenance and production operations.

## Guidelines
- Write scripts in Bash.
- Log each major step clearly so operators can follow progress.
- Fail fast with actionable error messages, especially for destructive commands.
- Keep scripts self-contained and scoped to the task they automate.
